---
deployment:
  tasks:
    # Domínio: geovannabnutri.com.br  ·  Usuário cPanel: thi75404
    - export DEPLOYPATH=/home2/thi75404/geovannabnutri.com.br
    - /bin/mkdir -p "$DEPLOYPATH/assets"
    - /bin/cp -f index.html "$DEPLOYPATH/"
    - /bin/cp -f .htaccess "$DEPLOYPATH/"
    - /bin/cp -Rf assets/. "$DEPLOYPATH/assets/"
